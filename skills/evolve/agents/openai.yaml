interface:
  display_name: "Evolve Skill"
  short_description: "Run explicit preflight-gated evolution loops"
  default_prompt: "Use $evolve to align the objective, scoring, evaluator, and cognition first, then drive the evolution loop round by round."

policy:
  allow_implicit_invocation: true
